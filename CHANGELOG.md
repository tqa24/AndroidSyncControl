# Changelog

All notable changes to this project are documented here.
This file is generated by [git-cliff](https://git-cliff.org) from Conventional Commits.

A **timeline read bottom-to-top**: each `## [M.m.0]` is the tag that opened a minor line; the
commits stacked ABOVE it are that line's successive versions `M.m.1`, `M.m.2`, ... (patch number =
commits since the tag), newest on top. Each line shows its version, conventional type, and message.

- 1.0.1 *(docs)* Introduce the product in README with screenshots and credits
## [1.0.0]

- *(ci)* Add GitHub release workflow [release]
- *(ci)* Add git-cliff changelog generation
- *(ci)* Add GitVersion for M.N.<commits> versioning
- *(feat)* Add device audio output to PC speakers via XAudio2
- *(feat)* Add app icon (two phones + sync) for exe and window
- *(feat)* Add System (follow OS) theme mode as default
- *(feat)* Replace Xceed IntegerUpDown with a themed NumericUpDown
- *(fix)* Allow dragging the window from the title bar when maximized
- *(feat)* Restyle MainWindow with KMonitor look and custom window chrome
- *(feat)* Add dark/light theme system with runtime switching
- *(build)* Target net8.0-windows instead of net462
- *(other)* Set Mouse and Key handle to true
## [Unreleased]

<!-- generated by git-cliff -->
